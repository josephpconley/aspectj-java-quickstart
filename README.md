# aspectj-java-quickstart
Hello world template for annotation-based AspectJ project in Java built by Maven

##Prerequisites
- Maven
- Java (setup to use Java 8 by default)

##Run
`mvn clean compile exec:java`